behaviours:

  CalibrationBehaviour:
    tick_phases: [Logic]
    receives: [Heartbeat, FanStatus, FanSample]
    sends: [FanCommand]

  FermentationBehaviour:
    tick_phases: [Logic]
    receives: [Heartbeat, ButtonStateChanged, ButtonRepeat, EncoderStep]
    sends: [FanCommand, DisplayCommand, HeatpadCommand, FermentationStatus]
