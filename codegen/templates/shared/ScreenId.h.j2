#pragma once
{% include "_common/header.j2" %}
#include <cstdint>

namespace Garbox {

enum class ScreenId : uint16_t {

    // null event
    Null = 0,

    // generated from config
{% for name in names %}
    {{ name.removesuffix("Screen") }},
{% endfor %}

    // enum size marker
    Count,
};

const char* ScreenIdToString(ScreenId id);

} // namespace Garbox
